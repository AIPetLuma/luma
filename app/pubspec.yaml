name: luma
description: An AI pet with autonomous life — "it runs while you're away."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Database
  sqflite: ^2.3.0
  path: ^1.8.0

  # Network & API
  dio: ^5.4.0
  supabase_flutter: ^2.3.0

  # State management
  flutter_riverpod: ^2.5.0
  riverpod_annotation: ^2.3.0

  # Background tasks
  workmanager: ^0.5.2

  # Firebase
  firebase_core: ^2.25.0
  firebase_messaging: ^14.7.0

  # Push notifications
  flutter_local_notifications: ^17.0.0

  # Analytics
  mixpanel_flutter: ^2.2.0

  # UI
  flutter_animate: ^4.5.0
  google_fonts: ^6.1.0

  # Platform
  url_launcher: ^6.2.0
  flutter_secure_storage: ^9.0.0

  # Utilities
  uuid: ^4.2.0
  intl: ^0.19.0
  shared_preferences: ^2.2.0
  equatable: ^2.0.5
  json_annotation: ^4.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.0
  json_serializable: ^6.7.0
  riverpod_generator: ^2.4.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/sounds/
